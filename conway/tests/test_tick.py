"""
Operates on a board.

Has a get-live-neigbour-count method, that takes a cell, and then finds the live neighbor count
It is that we use from tick and we mock it

We then add tests to board, to get the right count.

We might want to initialize the board via a mock

"""